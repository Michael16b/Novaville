name: Build and Deploy Novaville - Multi-Container

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: Azure Cloud
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 1. On télécharge l'image publique AVANT de se connecter à l'ACR
      # On utilise le tag '15' qui est le plus stable (rolling tag)
      - name: Pull Bitnami Postgres from Docker Hub
        run: docker pull bitnami/postgresql:15

      # 2. Connexion à votre Azure Container Registry
      - name: Log in to ACR
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.AZURE_ACR_NAME }}.azurecr.io
          username: ${{ secrets.AZURE_ACR_USERNAME }}
          password: ${{ secrets.AZURE_ACR_PASSWORD }}
          
      # 3. On pousse l'image Postgres vers VOTRE ACR
      - name: Push Bitnami Postgres to ACR
        run: |
          docker tag bitnami/postgresql:15 ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/bitnami-postgresql:15
          docker push ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/bitnami-postgresql:15

      # 4. Build et Push du Backend
      - name: Build and push Backend
        run: |
          docker build -t ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/novaville-backend:latest ./backend
          docker push ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/novaville-backend:latest

      # 5. Build et Push du Frontend
      - name: Build and push Frontend
        run: |
          docker build \
            --build-arg API_URL="https://novavilleapp.azurewebsites.net" \
            --build-arg ENVIRONMENT="production" \
            -t ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/novaville-frontend:latest ./frontend
          docker push ${{ secrets.AZURE_ACR_NAME }}.azurecr.io/novaville-frontend:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: Azure Cloud
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy Docker Compose
        run: |
          az webapp config container set \
            --name NovavilleApp \
            --resource-group Novaville \
            --multicontainer-config-type COMPOSE \
            --multicontainer-config-file docker-compose-azure.yml \
            --docker-registry-server-url https://${{ secrets.AZURE_ACR_NAME }}.azurecr.io \
            --docker-registry-server-user ${{ secrets.AZURE_ACR_USERNAME }} \
            --docker-registry-server-password ${{ secrets.AZURE_ACR_PASSWORD }}